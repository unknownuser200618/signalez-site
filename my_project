<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signalez - Comportement Suspect</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 90%;
            max-width: 600px;
            margin: 20px auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
        }
        h1 {
            text-align: center;
            color: #333;
        }
        label {
            display: block;
            margin: 10px 0 5px;
            color: #555;
        }
        input[type="text"],
        input[type="datetime-local"],
        textarea,
        select {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border-radius: 5px;
            border: 1px solid #ddd;
            transition: border-color 0.3s;
        }
        input[type="text"]:focus,
        input[type="datetime-local"]:focus,
        textarea:focus,
        select:focus {
            border-color: #5c67f2;
        }
        .submit-btn {
            background-color: #5c67f2;
            color: white;
            padding: 10px;
            border: none;
            width: 100%;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .submit-btn:hover {
            background-color: #434fc7;
        }
        .error-message {
            color: red;
            margin-top: 5px;
        }
        #map {
            height: 300px;
            margin-top: 20px;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
        }
        .feedback-container {
            display: none; /* Caché par défaut */
            margin-top: 20px;
        }
        .faq-section {
            margin-top: 30px;
        }
        .faq-item {
            margin-bottom: 15px;
        }
        .info-message {
            background-color: #e7f3fe;
            color: #31708f;
            padding: 15px;
            margin: 10px 0;
            border: 1px solid #bce8f1;
            border-radius: 5px;
        }
    </style>
</head>
<body>

<div class="container">
    <h1>Signalez - Comportement Suspect</h1>
    
    <!-- Informations sur la géolocalisation -->
    <div class="info-message">
        <strong>Géolocalisation</strong><br>
        Nous demandons l'accès à votre localisation pour vous aider à signaler plus précisément les comportements suspects. Votre position sera utilisée uniquement pour remplir le champ de localisation. Les résultats peuvent varier selon les paramètres de votre appareil.
        <br><br>
        Consultez notre <a href="#" style="color: #5c67f2;">politique de confidentialité</a> pour plus d'informations sur la gestion de vos données.
    </div>

    <form id="reportForm">
        <label for="behavior">Type de comportement :</label>
        <select id="behavior" required>
            <option value="" disabled selected>Sélectionnez un comportement</option>
            <option value="attouchements">Attouchements</option>
            <option value="regard">Regard pervers</option>
            <option value="sifflement">Sifflements</option>
            <option value="autre">Autre (précisez ci-dessous)</option>
        </select>
        
        <label for="otherBehavior">Autre comportement :</label>
        <input type="text" id="otherBehavior" placeholder="Précisez le comportement" style="display:none;">

        <label for="location">Localisation :</label>
        <input type="text" id="location" placeholder="Ex : Rue de Rivoli, Paris" required>
        <button type="button" id="getLocation" class="submit-btn" style="margin-top: 10px;">Utiliser ma localisation</button>
        <div class="error-message" id="locationError"></div>

        <label for="datetime">Date et heure :</label>
        <input type="datetime-local" id="datetime" required>

        <label for="description">Description (facultatif) :</label>
        <textarea id="description" rows="4" placeholder="Décrire l'incident"></textarea>

        <div id="map"></div>

        <button type="submit" class="submit-btn">Envoyer le signalement</button>
        <div class="error-message" id="formError"></div>
    </form>

    <div class="feedback-container" id="feedbackContainer">
        <h3>Évaluation de l'expérience</h3>
        <label for="feedback">Comment évalueriez-vous votre expérience de signalement ?</label>
        <select id="feedback">
            <option value="" disabled selected>Sélectionnez une évaluation</option>
            <option value="excellent">Excellent</option>
            <option value="bon">Bon</option>
            <option value="moyen">Moyen</option>
            <option value="mauvais">Mauvais</option>
        </select>
        <button id="submitFeedback" class="submit-btn">Envoyer le retour</button>
    </div>

    <div class="faq-section">
        <h3>Questions Fréquemment Posées (FAQ)</h3>
        <div class="faq-item">
            <strong>Comment signaler un comportement ?</strong>
            <p>Remplissez le formulaire avec les détails de l'incident et cliquez sur "Envoyer le signalement".</p>
        </div>
        <div class="faq-item">
            <strong>Que se passe-t-il après un signalement ?</strong>
            <p>Votre signalement sera traité par nos équipes. Vous recevrez un identifiant unique pour le suivi de votre signalement.</p>
        </div>
        <div class="faq-item">
            <strong>Pourquoi est-il important de signaler ?</strong>
            <p>Signaler les comportements suspects aide à créer une communauté plus sûre. Chaque signalement compte.</p>
        </div>
    </div>
</div>

<script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
<script>
    const apiKey = '7ea75c07538a4a18896d0c50afe834dd'; // Ta clé API OpenCage

    // Initialiser la carte
    const map = L.map('map').setView([48.8566, 2.3522], 13); // Centre sur Paris

    // Ajouter une couche de carte
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        maxZoom: 19,
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
    }).addTo(map);

    // Fonction pour ajouter un marqueur
    function addMarker(lat, lon) {
        map.setView([lat, lon], 13); // Centrer la carte sur la localisation
        L.marker([lat, lon]).addTo(map).bindPopup('Signalement ici').openPopup();
    }

    // Événement pour changer le type de comportement
    document.getElementById('behavior').addEventListener('change', function() {
        const otherBehaviorInput = document.getElementById('otherBehavior');
        if (this.value === 'autre') {
            otherBehaviorInput.style.display = 'block';
            otherBehaviorInput.required = true; // Rendre ce champ requis
        } else {
            otherBehaviorInput.style.display = 'none';
            otherBehaviorInput.required = false; // Rendre ce champ non requis
        }
    });

    // Événement pour le bouton de géolocalisation
    document.getElementById('getLocation').addEventListener('click', function() {
        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(function(position) {
                const lat = position.coords.latitude;
                const lon = position.coords.longitude;

                // Ajouter le marqueur sur la carte et remplir le champ de localisation
                addMarker(lat, lon);
                document.getElementById('location').value = `Lat: ${lat}, Lon: ${lon}`; // Afficher les coordonnées
            }, function(error) {
                switch(error.code) {
                    case error.PERMISSION_DENIED:
                        alert('Vous avez refusé la demande de géolocalisation. Veuillez autoriser l\'accès.');
                        break;
                    case error.POSITION_UNAVAILABLE:
                        alert('La position n\'est pas disponible. Vérifiez vos paramètres de localisation.');
                        break;
                    case error.TIMEOUT:
                        alert('La demande de géolocalisation a expiré. Veuillez réessayer.');
                        break;
                    case error.UNKNOWN_ERROR:
                        alert('Une erreur inconnue s\'est produite.');
                        break;
                }
            });
        } else {
            alert('La géolocalisation n\'est pas prise en charge par ce navigateur.');
        }
    });

    // Événement pour l'envoi du formulaire
    document.getElementById('reportForm').addEventListener('submit', function(event) {
        event.preventDefault(); // Empêcher le rechargement de la page

        const behavior = document.getElementById('behavior').value;
        const otherBehavior = document.getElementById('otherBehavior').value;
        const location = document.getElementById('location').value;
        const datetime = document.getElementById('datetime').value;
        const description = document.getElementById('description').value;

        // Si 'autre' a été sélectionné, utiliser le texte fourni
        const finalBehavior = behavior === 'autre' ? otherBehavior : behavior;

        if (!location) {
            document.getElementById('locationError').textContent = 'Veuillez indiquer une localisation.';
            return;
        }

        // Afficher un message de confirmation
        showConfirmation(finalBehavior, location, datetime, description);
    });

    function showConfirmation(behavior, location, datetime, description) {
        const confirmationMessage = `
            <h3>Signalement soumis</h3>
            <p><strong>Comportement :</strong> ${behavior}</p>
            <p><strong>Localisation :</strong> ${location}</p>
            <p><strong>Date et heure :</strong> ${datetime}</p>
            <p><strong>Description :</strong> ${description || 'Aucune description fournie.'}</p>
            <p>Vous pouvez suivre votre signalement avec l'identifiant unique : <strong>${Math.random().toString(36).substr(2, 9)}</strong>.</p>
        `;
        document.querySelector('.container').innerHTML += confirmationMessage;
        document.getElementById('reportForm').reset();
        document.getElementById('feedbackContainer').style.display = 'block'; // Afficher le formulaire de feedback
    }

    // Événement pour le retour d'expérience
    document.getElementById('submitFeedback').addEventListener('click', function() {
        const feedbackValue = document.getElementById('feedback').value;
        if (feedbackValue) {
            alert('Merci pour votre retour d\'expérience !');
            document.getElementById('feedbackContainer').style.display = 'none'; // Cacher le formulaire de feedback
        } else {
            alert('Veuillez sélectionner une évaluation.');
        }
    });
</script>
</body>
</html>
